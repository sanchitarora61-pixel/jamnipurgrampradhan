<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>Submit Complaint - Jamnipur</title>
  <style>
    body {
      font-family: Arial;
      padding: 20px;
      background: #f4f4f4;
    }

    h2 {
      color: #2e7d32;
    }

    form {
      background: white;
      padding: 20px;
      border-radius: 10px;
      max-width: 500px;
      margin: auto;
    }

    input,
    select,
    textarea {
      width: 100%;
      padding: 10px;
      margin: 10px 0;
      border-radius: 5px;
      border: 1px solid #ccc;
    }

    button {
      background: #2e7d32;
      color: white;
      padding: 10px 20px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }
  </style>
</head>

<body>

  <h2>Submit Your Complaint</h2>
  <form id="complaintForm">
    <label for="name">Full Name:</label>
    <input type="text" id="name" name="name" required>

    <label for="phone">Phone Number:</label>
    <input type="tel" id="phone" name="phone" required>

    <label for="category">Category:</label>
    <select id="category" name="category">
      <option>Water Supply</option>
      <option>Road Damage</option>
      <option>Street Lights</option>
      <option>Garbage Collection</option>
      <option>Other</option>
    </select>

    <label for="details">Complaint Details:</label>
    <textarea id="details" name="details" rows="5" required></textarea>

    <button type="submit">Submit Complaint</button>
  </form>
  <form action="https://script.google.com/macros/s/AKfycbxhmnUbQDjAcxj4l-ovqsia6C87krwM4rtrr4WHqr7A5_gT6ptPKe5wf6BEeUE_vWLCWw/exec" method="POST" id="complaintForm">
    
    method: 'POST',
    body: JSON.stringify(data),
    headers: {
      'Content-Type': 'application/json'
  <script>
    document.getElementById('complaintForm').addEventListener('submit', function (e) {
      e.preventDefault();
      // Generate a simple tracking ID (e.g., JAMNIPUR-XXXXXX)
      const trackingId = 'JAMNIPUR-' + Math.floor(100000 + Math.random() * 900000);
      // Save tracking ID to localStorage (or pass via URL)
      localStorage.setItem('trackingId', trackingId);
      // Redirect to thank you page
      window.location.href = 'thankyou.html';
    });
  </script>

</body>

</html>
